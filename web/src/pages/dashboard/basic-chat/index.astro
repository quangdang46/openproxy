---
// Intentional orphan route (parity with 9router): page is reachable by direct
// URL for smoke/debug, but deliberately NOT linked in Sidebar nav. Do not add
// to navItems/footerItems without a product decision.
import Layout from '@/layouts/Layout.astro';
import BasicChatPageClient from '@/components/BasicChatPageClient';
import DashboardLayout from '@/shared/components/layouts/DashboardLayout';
---
<Layout>
  <DashboardLayout client:only="react">
    <BasicChatPageClient client:only="react" />
  </DashboardLayout>
</Layout>