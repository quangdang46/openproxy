# @openprx/openproxy-darwin-arm64

Platform binary for OpenProxy on darwin arm64.

You should not install this package directly. Use the meta package instead:

```bash
npm install -g @openprx/openproxy
```

Your package manager will pick the correct platform package automatically.

Repository: https://github.com/quangdang46/openproxy
